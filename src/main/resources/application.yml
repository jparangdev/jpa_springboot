spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/h2/database/jpa-springboot
    username: sa
    password:
    drivcer-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
      properites:
        hibernate:
#          show_sql: true
          format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace